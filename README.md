A dynamic neuron network implemented in C++ from scratch without any third-party dependencies, and use [bazel](https://www.bazel.build/) to build and run:

``` bash
bazel run //nn-from-scratch:main
```

Train with debug info:
``` bash
bazel run //nn-from-scratch:main_debug
```

